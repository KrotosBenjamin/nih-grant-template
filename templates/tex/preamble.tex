% pandoc/LaTeX template for NIH grant pdfs (minimal)
\usepackage[scaled]{helvet}
\renewcommand{\familydefault}{\sfdefault}
\usepackage{setspace}
\setstretch{1.0} % single-spaced body
\usepackage{geometry}

% page geometry (safe default: 0.5in)
\usepackage[margin=0.5in]{geometry}

% Helvetica ≈ Arial; satisfies many FOA requirements—verify for your FOA.
% margins set via _quarto.yml geometry
\usepackage{microtype}
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
% optional header with PI + mechanism
\fancyhead[L]{\textit{PI Name}}
\fancyhead[R]{\textit{\MakeUppercase{\thetitle}}}

% Optional DRAFT watermark controlled by param
$if(params.draft)$
\usepackage[nostamp]{draftwatermark}
\SetWatermarkText{DRAFT}
\SetWatermarkScale{0.9}
\SetWatermarkLightness{0.90}
$endif$

% % line spacing and paragraphing
% \usepackage{setspace}
% $if(linestretch)$\setstretch{$linestretch$}$else$\setstretch{1.05}$endif$
% \setlength{\parskip}{0.3\baselineskip}
% \setlength{\parindent}{0pt}

% % url formatting
% \usepackage[hidelinks]{hyperref}
% \usepackage{microtype}

% % headings: small and tight
% \usepackage{titlesec}
% \titlespacing*{\section}{0pt}{0.5ex}{0.2ex}
% \titlespacing*{\subsection}{0pt}{0.5ex}{0.2ex}
% \titleformat{\section}{\bfseries\normalsize}{}{0pt}{}
% \titleformat{\subsection}{\itshape\normalsize}{}{0pt}{}

% \begin{document}
% $body$
% \end{document}
